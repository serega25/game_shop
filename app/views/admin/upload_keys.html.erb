<%= form_with(model: GameKey.new, url: import_keys_admin_game_keys_path, local: true, multipart: true) do |form| %>
  <%= form.file_field :json_file, accept: 'application/json' %>
  <%= form.submit 'Upload' %>
<% end %>
